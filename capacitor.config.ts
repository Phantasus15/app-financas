import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app-financas',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
